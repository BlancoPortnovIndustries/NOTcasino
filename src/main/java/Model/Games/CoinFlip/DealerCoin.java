package Model.Games.CoinFlip;

import Model.AbstractGameClasses.Player;
import Model.Server.ClientThread;

public class DealerCoin extends Player {
    public DealerCoin(ClientThread clientThread) {
        super(clientThread);
    }
}
